#!/usr/bin/env bash
set -e
test -z "${DEBUG}" || set -x

##
# parameters
: "${REPO:=$HOME/git}"
opts="Dhps"
doPrint=false
doShort=false

function usage {
  cat << _usage_
  $(basename "$0"): copy commit sha to pasteboard

  usage:
    $(basename "$0") [-$opts] [repo]

    D - turn on debug mode.
    h - you are here.
    p - print to stdout (default: $doPrint).
    s - short commit sha.
_usage_
}

while getopts $opts opt; do
  case $opt in
    D)   DEBUG=true; set -x  ;;
    h)   usage; exit         ;;
    p)   doPrint=true        ;;
    s)   doShort=true        ;;
    ?|*) echo; usage; exit 5 ;;
  esac
done
shift $((OPTIND-1))
##


##
# main
context=.
if test -n "$1"; then
  test -z "$GH_ORG" || org=/$GH_ORG
  dir=$REPO${org}/$1
  test -d "$dir" || { echo "$dir not found!" >&2; exit 1; }
  context="$dir"
fi

if $doShort; then
  sha="$(git -C "$context" rev-parse --short HEAD)"
else
  sha="$(git -C "$context" rev-parse HEAD)"
fi

if $doPrint; then
  printf '%s\n' "$sha"
else
  printf '%s' "$sha" | clip
fi
##
